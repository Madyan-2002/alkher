import 'package:flutter/material.dart';

class FavoriteScreenUser extends StatelessWidget {
  const FavoriteScreenUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}