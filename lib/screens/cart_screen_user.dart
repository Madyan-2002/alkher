import 'package:flutter/material.dart';

class CartScreenUser extends StatelessWidget {
  const CartScreenUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}